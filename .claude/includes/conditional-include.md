<!--
Helper: Conditionally include a file if a marker/flag appears in RAW_ARGS.
Usage (inside a command):
- Define RAW_ARGS = $ARGUMENTS
- Ask the model to check for presence of a token (e.g., --interactive-response)
- If present, include the referenced section/file verbatim below
-->
